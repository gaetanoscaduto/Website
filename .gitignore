.Rproj.user
.Rhistory
.RData
.Ruserdata

/.quarto/

**/*.quarto_ipynb

# Quarto build output. Regenerated by `quarto render` and by the GitHub Action
# on every run, so it must never be committed: stale files accumulate in here
# and get re-uploaded to Quarto Pub, which is what caused the 413 publish errors.
/_site/
